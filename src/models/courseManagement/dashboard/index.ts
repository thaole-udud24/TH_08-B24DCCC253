import type { DashboardModelState } from '../typing';

const initialState: DashboardModelState = {};

export default {
  namespace: 'courseManagement.dashboard',
  state: initialState,
  reducers: {},
};